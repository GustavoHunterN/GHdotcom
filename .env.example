# Copia este archivo a .env y rellena los valores (nunca subas .env al repositorio).

# Token de GitHub con alcance repo (https://github.com/settings/tokens)
GITHUB_TOKEN=

# MySQL
DB_HOST=localhost
DB_USER=root
DB_PSSWD=
DB_NAME=GH
